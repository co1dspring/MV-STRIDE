# misc

无关 / 辅助代码归档。非核心数据构建逻辑，但有时仍需参考或复用。

内部按用途分子目录：

- `visualization/` — 交互式浏览 / 绘图工具（PyQt5 QA 浏览器、训练日志图、Streamlit 人工标注 / QC 工具）。
- `legacy/` — 过期 / 早期版本代码（保留作参考，不推荐使用）。

> 归档完成：文件自原 `scripts/`、`data_test_sampling/`、`data_qc_sampling/`、`dataset_generation/` 迁入。
