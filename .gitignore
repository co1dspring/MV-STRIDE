# ---------- 私有 / 内部内容（不公开） ----------
# 开发笔记（保留在服务器本地，不进入公开仓库）
note/

# 场景生成过程日志（Infinigen 生成记录，保留本地）
development_log/

# Claude 会话记录与内部配置
claude_log.md
.claude/

# Qoder 本地会话配置
.qoder/

# 本地复现数据：QA 输入与输出（发布到 HuggingFace，不入 GitHub）
data/*
!data/README.md

# ---------- 环境与凭据 ----------
# 环境变量 / 密钥文件（防止误提交）
.env
.env.*
*.pem
*.key

# ---------- Python ----------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# ---------- 系统 / 编辑器 ----------
.DS_Store
*.swp
*~
